import { sql } from "drizzle-orm";
import { index, integer, sqliteTable, text, uniqueIndex } from "drizzle-orm/sqlite-core";
export const categories=sqliteTable("categories",{id:integer("id").primaryKey({autoIncrement:true}),name:text("name").notNull(),slug:text("slug").notNull(),description:text("description").notNull().default(""),sortOrder:integer("sort_order").notNull().default(0)},t=>[uniqueIndex("idx_categories_name").on(t.name),uniqueIndex("idx_categories_slug").on(t.slug)]);
export const posts=sqliteTable("posts",{id:integer("id").primaryKey({autoIncrement:true}),title:text("title").notNull(),slug:text("slug").notNull(),excerpt:text("excerpt").notNull().default(""),body:text("body").notNull().default(""),category:text("category").notNull(),tagsJson:text("tags_json").notNull().default("[]"),status:text("status").notNull().default("draft"),publishedAt:text("published_at"),scheduledAt:text("scheduled_at"),readingMinutes:integer("reading_minutes").notNull().default(5),visual:text("visual").notNull().default("NEW"),createdAt:text("created_at").notNull().default(sql`CURRENT_TIMESTAMP`),updatedAt:text("updated_at").notNull().default(sql`CURRENT_TIMESTAMP`)},t=>[uniqueIndex("idx_posts_slug").on(t.slug),index("idx_posts_status_published").on(t.status,t.publishedAt),index("idx_posts_category").on(t.category)]);
export const postingQueue=sqliteTable("posting_queue",{id:integer("id").primaryKey({autoIncrement:true}),postId:integer("post_id").notNull().references(()=>posts.id),status:text("status").notNull().default("waiting"),sourceUrl:text("source_url"),scheduledAt:text("scheduled_at"),attempts:integer("attempts").notNull().default(0),lastError:text("last_error"),createdAt:text("created_at").notNull().default(sql`CURRENT_TIMESTAMP`)},t=>[index("idx_queue_status_scheduled").on(t.status,t.scheduledAt)]);
export const siteSettings=sqliteTable("site_settings",{key:text("key").primaryKey(),valueJson:text("value_json").notNull(),updatedAt:text("updated_at").notNull().default(sql`CURRENT_TIMESTAMP`)});
export const contentAgents=sqliteTable("content_agents",{id:text("id").primaryKey(),name:text("name").notNull(),category:text("category").notNull(),mission:text("mission").notNull(),status:text("status").notNull().default("active"),cadenceHours:integer("cadence_hours").notNull().default(168),sourcesJson:text("sources_json").notNull().default("[]"),topicsJson:text("topics_json").notNull().default("[]"),videoJson:text("video_json"),nextRunAt:text("next_run_at"),lastRunAt:text("last_run_at"),topicCursor:integer("topic_cursor").notNull().default(0),updatedAt:text("updated_at").notNull().default(sql`CURRENT_TIMESTAMP`)},t=>[index("idx_content_agents_status_next").on(t.status,t.nextRunAt)]);
export const agentRuns=sqliteTable("agent_runs",{id:integer("id").primaryKey({autoIncrement:true}),agentId:text("agent_id").notNull().references(()=>contentAgents.id),status:text("status").notNull(),topic:text("topic").notNull(),postId:integer("post_id").references(()=>posts.id),message:text("message"),createdAt:text("created_at").notNull().default(sql`CURRENT_TIMESTAMP`)},t=>[index("idx_agent_runs_agent_created").on(t.agentId,t.createdAt)]);
