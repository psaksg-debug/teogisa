import type { Metadata } from "next";
import { InnerHeader, SiteFooter } from "../components/SiteChrome";
import { liveKeywordPages } from "../../lib/portal";

export const metadata:Metadata={title:"지역·세부키워드 사이트빌더",description:"지역명과 세부 질문을 결합한 유용한 정보 페이지의 운영 현황을 확인합니다.",alternates:{canonical:"/keyword-lab"}};
export default function KeywordLab(){return <><InnerHeader eyebrow="PROGRAMMATIC CONTENT LAB" title="지역·세부키워드 사이트빌더" description="키워드만 바꾼 빈 페이지는 만들지 않습니다. 지역 공식 창구, 신청 경로와 독자 질문이 달라질 때만 별도 페이지를 운영합니다."/><main className="content-shell portal-shell"><section className="builder-flow"><div><span>01</span><strong>핵심 질문 분해</strong><p>퇴직·지원금·일자리·부업 질문을 세부 의도로 나눕니다.</p></div><div><span>02</span><strong>지역 정보 결합</strong><p>지자체·고용센터·공공기관의 고유 정보를 확인합니다.</p></div><div><span>03</span><strong>중복 검토</strong><p>기존 글과 실질적으로 다른 내용이 없으면 생성하지 않습니다.</p></div><div><span>04</span><strong>운영 현황 공개</strong><p>발행·검토·업데이트 대상을 관리자에서 추적합니다.</p></div></section><section className="live-pages"><div className="section-heading"><div><p className="eyebrow">LIVE KEYWORD PAGES</p><h2>운영 중인 페이지</h2></div><span>{liveKeywordPages.length}개 시범 운영</span></div>{liveKeywordPages.map(page=><a href={`/local/${encodeURIComponent(page.region)}/${page.topic}`} key={page.label}><span>운영 중</span><strong>{page.label}</strong><small>/{page.region}/{page.topic}</small><b>페이지 보기 →</b></a>)}</section><aside className="safety-note"><strong>대량 생성 제한</strong><p>지역명만 바꾼 문서, 공식 출처가 없는 문서, 독자에게 추가 가치가 없는 문서는 검색 노출용으로 자동 생성하지 않습니다.</p></aside></main><SiteFooter/></>}
