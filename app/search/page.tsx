import Link from "next/link";import {getPublishedPosts} from "../../lib/repository";
export default async function SearchPage({searchParams}:{searchParams:Promise<{q?:string;category?:string}>}){const {q="",category=""}=await searchParams;const all=await getPublishedPosts();const posts=all.filter(p=>(!q||`${p.title} ${p.excerpt} ${p.tags.join(" ")}`.toLowerCase().includes(q.toLowerCase()))&&(!category||p.category===category));return <><header className="inner-header"><Link className="brand" href="/"><span className="brand-mark">퇴</span><span>퇴직하고 부자되기</span></Link><p className="eyebrow" style={{marginTop:60}}>CONTENT SEARCH</p><h1>필요한 글 찾기</h1><form className="search-form"><input name="q" defaultValue={q} placeholder="실업급여, 부업, AI처럼 검색하세요"/><button>검색</button></form></header><main className="content-shell"><p>{posts.length}개의 글을 찾았습니다.</p><div className="admin-list">{posts.map(p=><Link className="admin-item" href={`/posts/${p.slug}`} key={p.slug}><p>{p.title}</p><span>{p.category} · {p.excerpt}</span></Link>)}</div></main></>}
