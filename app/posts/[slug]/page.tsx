import type { Metadata } from "next";
import Link from "next/link";
import { notFound } from "next/navigation";
import { getPost } from "../../../lib/repository";
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const {slug}=await params;const post=await getPost(slug);return post?{title:post.title,description:post.excerpt,alternates:{canonical:`/posts/${post.slug}`},openGraph:{type:"article",title:post.title,description:post.excerpt,publishedTime:post.publishedAt}}:{};}
function renderBody(body:string){return body.split(/\n(?=## )/).map((part,i)=>{const [first,...rest]=part.split("\n");return first.startsWith("## ")?<section key={i}><h2>{first.slice(3)}</h2><p>{rest.join("\n").trim()}</p></section>:<p key={i}>{part.trim()}</p>});}
export default async function PostPage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const post=await getPost(slug);if(!post)notFound();const jsonLd={"@context":"https://schema.org","@type":"Article",headline:post.title,datePublished:post.publishedAt,description:post.excerpt,author:{"@type":"Organization",name:"퇴직하고 부자되기"},publisher:{"@type":"Organization",name:"퇴직하고 부자되기"}};return <><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify(jsonLd)}}/><header className="inner-header"><Link className="brand" href="/"><span className="brand-mark">퇴</span><span>퇴직하고 부자되기</span></Link><p className="eyebrow" style={{marginTop:60}}>{post.category} · {post.readingMinutes}분</p><h1>{post.title}</h1><p>{post.excerpt}</p></header><main className="content-shell"><Link className="back-link" href="/">← 홈으로</Link><div className="article-layout"><article className="article-copy">{renderBody(post.body)}</article><aside className="article-aside"><strong>발행 정보</strong><p>{post.publishedAt}</p><strong>관련 태그</strong><div>{post.tags.map(t=><span className="chip" key={t}>#{t}</span>)}</div></aside></div></main></>}
