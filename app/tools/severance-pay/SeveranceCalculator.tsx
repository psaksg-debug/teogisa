"use client";
import { useMemo, useState } from "react";
export default function SeveranceCalculator(){const [wage,setWage]=useState(3500000);const [days,setDays]=useState(3650);const amount=useMemo(()=>Math.max(0,Math.round((wage/30)*30*(days/365))),[wage,days]);return <section className="simple-calculator"><div className="calculator-inputs"><h2>계산 조건</h2><label>최근 3개월 기준 월 평균임금<input type="number" min="0" step="10000" value={wage} onChange={e=>setWage(Number(e.target.value))}/><span>원</span></label><label>계속근로기간<input type="number" min="365" value={days} onChange={e=>setDays(Number(e.target.value))}/><span>일</span></label></div><div className="calculator-output"><span>예상 퇴직금</span><strong>{amount.toLocaleString("ko-KR")}원</strong><p>간편 추정값입니다. 상여금·연차수당·평균임금 산정 제외기간 등에 따라 실제 금액이 달라질 수 있습니다.</p></div></section>}
